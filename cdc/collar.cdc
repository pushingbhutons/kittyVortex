// CryptoKitty needs to carry zStar with them (same account) in order to open Kitty Vortex
// zStar is NFT resource
// zCollar is resource that holds zStar (collar + zStar = zCollar)
// zCollar without zStar is collar with empty jewel hole, requires forging (zcollar + zstar), once forged (parts burned, forged item created)
//    - Forge a CryptoKitty zCollar
//        - Green Collar with Two Gem Slots
//        - 2x white dwarf zStar
//        - 1x hp 5% bonus
//        - makes Green zCollar with White Dwarf Gems (+5% HP bonus)
