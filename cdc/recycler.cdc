// forged NFT is burnt
// NFTs used in forge (minus qualifier) called by recycler are released to player address that owns & used the recycler
// recycled NFTs have lower dmg, hp, xp
// recycled items can be used in forge, increased likelihood of forge failure (charity option - we don't burn the item, we donate it to the drop system for random drops)
